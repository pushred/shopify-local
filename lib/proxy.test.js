const Lab = require('lab');

const lab = exports.lab = Lab.script();

lab.test('skips cache if no-cache cache-control is set, shift-refresh', function (done) {
  done();
});

lab.test('strips CSP/XSS headers to avoid browser warnings on a different domain', function (done) {
  done();
});

lab.test('caches text/HTML responses only', function (done) {
  done();
});

lab.test('decodes gzip-encoded response bodies before caching', function (done) {
  done();
});

lab.test('points theme asset references to local copy', function (done) {
  done();
});
